import Meta from "@/components/Meta";

<Meta pageTitle="Requirements | Service Node" description="Requirements | Service Node" />





# Requirements

##

**Docker** must be installed on the system.

**Backend Engine** Plugin Instance must be installed on into your gluestack project. To add one, you can use the below command:

```sh
$ node glue add backend-engine engine
```