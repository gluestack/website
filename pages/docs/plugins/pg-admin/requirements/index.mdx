import Meta from "@/components/Meta";

<Meta pageTitle="Requirements | PG-Admin" description="Requirements | PG-Admin." />





# Requirements

##

**Docker** must be installed on the system.

**Postgres** plugin Instance. You can install it using the following command:

```sh
$ node glue add postgres postgres
```
