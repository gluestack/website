import Meta from "@/components/Meta";

<Meta pageTitle="Requirements | Auth" description="Requirements | Auth" />





# Requirements

##

<br/>

* Backend-Engine Plugin Instance
    
    ```bash
    $ node glue add backend-engine engine
    ```

<br/>

* Postgres Plugin Instance
    
    ```bash
    $ node glue add postgres postgres
    ```

<br/>

* Graphql Plugin Instance
    
    ```bash
    $ node glue add graphql graphql
    ```
